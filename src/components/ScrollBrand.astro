---
import MySQLIcon from "src/icons/MySQLIcon.astro";
import ReactIcon from "src/icons/ReactIcon.astro";
import AstroIcon from "src/icons/AstroIcon.astro";
import FigmaIcon from "src/icons/FigmaIcon.astro";
import CSSIscon from "src/icons/CSSIscon.astro";
import HTMLIcon from "src/icons/HTMLIcon.astro";
import TailwindIcon from "src/icons/TailwindIcon.astro";
import Bashlicons from "src/icons/Bashlicons.astro";
import GitbashIcons from "src/icons/GitbashIcons.astro";
import CiscoIcon from "src/icons/CiscoIcon.astro";
import ExpressIcons from "src/icons/ExpressIcons.astro";
import JavaScriptIcon from "src/icons/JavaScriptIcon.astro";
---

<section>
  <div class="scroller mx-auto max-w-[1280px] mt-12">
    <ul
      class="tag-list scroller__inner flex gap-4 md:gap-6 py-4 m-0 p-0 list-none"
    >
      <li class="p-3 md:p-4">
        <ReactIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <AstroIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <ExpressIcons class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <FigmaIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <MySQLIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <TailwindIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <CSSIscon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <HTMLIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <JavaScriptIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <CiscoIcon class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <Bashlicons class="w-10 h-10 md:w-12 md:h-12" />
      </li>
      <li class="p-3 md:p-4">
        <GitbashIcons class="w-10 h-10 md:w-12 md:h-12" />
      </li>
    </ul>
  </div>
</section>

<style>
  .scroller {
    max-width: 980px;
  }

  .tag-list {
    margin: 0;
    padding-inline: 0;
    list-style: none;
  }

  /* El resto del CSS se mantiene igual, solo se migraron los media queries a Tailwind */
  .scroller[data-animated="true"] {
    overflow: hidden;
    mask: linear-gradient(
      90deg,
      transparent,
      white,
      30%,
      white 80%,
      transparent
    );
  }
  .scroller[data-animated="true"] .scroller__inner {
    width: max-content;
    flex-wrap: nowrap;
    animation: scroll 32s linear infinite;
  }

  @keyframes scroll {
    to {
      transform: translate(calc(-50% - 0.5rem));
    }
  }
</style>

<script is:inline>
  // Si un usuario no ha optado por reduced motion, entonces agregamos la animación
  if (!window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    addAnimation();
  }

  function addAnimation() {
    const scrollers = document.querySelectorAll(".scroller");
    scrollers.forEach((scroller) => {
      // add data-animated="true" to every `.scroller` on the page
      scroller.setAttribute("data-animated", true);

      // Make an array from the elements within `.scroller__inner`
      const scrollerInner = scroller.querySelector(".scroller__inner");
      const scrollerContent = Array.from(scrollerInner.children);

      scrollerContent.forEach((item) => {
        const duplicatedItem = item.cloneNode(true);
        duplicatedItem.setAttribute("aria-hidden", true);
        scrollerInner.appendChild(duplicatedItem);
      });
    });
  }
</script>
